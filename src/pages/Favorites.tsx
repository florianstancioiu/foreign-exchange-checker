import TabsMenu from "../components/TabsMenu/TabsMenu";

const Favorites = () => {
  return (
    <TabsMenu variant="favorites">
      <h1>Favorites</h1>
    </TabsMenu>
  );
};

export default Favorites;
