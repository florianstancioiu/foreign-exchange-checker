# Issues

Things I noticed that need addressing

- Check again how I retrieve and process the liveMarkets data, it might be incorrect
- Add tests for root level components
- Add tests for pages
- Add tests for contexts
- Add test coverage
- ~~Structure the pages components better~~
- ~~Update `useQuery` calls to use proper queryKeys~~
- ~~Add meta tags with image and description of the website so that when you share the LIVE url, users can see a preview~~
- ~~The send value and currency (as well as the receive value and currency) needs to be stored and loaded from the URL~~
- ~~The receive value is 0 inside the url all the time, either remove it or make it change its value~~
- ~~Clicking on the CompareItem star needs to be functional~~
- ~~Clicking on the FavoriteItem needs to load firstCurrency and secondCurrency into CheckRate component~~
- ~~The conversion RateLog date needs to be formatted to something like `June 27`~~
- ~~There is an issue when selecting the same Currency in the CheckRate component~~
- ~~The Send input acts weird when you type values~~
